# 🛡️ الأمان: اللي اتعمل لحد دلوقتي

> ده تقرير مرحلي. المراجعة الأمنية الشاملة والتقرير النهائي في المرحلة الخامسة.

## الحمايات الموجودة

| الخطر                                   | الحماية                                                                                                                                                  | فين                                                          |
| --------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| شركة تشوف بيانات شركة تانية             | كل جدول عليه ختم الشركة، وقاعدة البيانات نفسها (Row-Level Security) بتخفي أي صف مش تبعها، حتى لو في غلطة في الكود. حساب السيرفر مايقدرش يعدّي الحماية دي | `apps/api/drizzle/0001_security.sql`، `DbService.withTenant` |
| مستخدم يشوف أو يعدّل حاجة مش بتاعته     | كل مسار مقفول افتراضياً ولازم يحدد الأدوار المسموحة. كل طلب بيتفلتر بصاحبه (العميل طلباته، المحل طلباته، الطيار المسند له)                               | `auth.guard.ts`، `orders.service.ts`                         |
| تخمين كلمات السر أو أكواد التحقق        | حد للمحاولات لكل جهاز ولكل رقم، وقفل الحساب ١٥ دقيقة بعد ٥ محاولات غلط، والكود بيتقفل بعد ٥ محاولات ويخلص بعد ٥ دقايق                                    | `rate-limit.service.ts`، `auth.service.ts`                   |
| سرقة كلمات السر من القاعدة              | مشفرة بـ argon2id (أقوى طريقة موصى بيها حالياً). أكواد التحقق متخزنة مشفرة                                                                               | `auth.service.ts`                                            |
| سرقة الجلسة                             | توكن قصير (١٥ دقيقة) في الذاكرة بس، والطويل في كوكي مقفولة مايقدرش الجافاسكريبت يقراها. كل تجديد بيلغي القديم، ولو القديم اتستخدم تاني كل الجلسات بتتقفل | `auth.controller.ts`                                         |
| تزوير الطلبات من موقع تاني (CSRF)       | كوكي `SameSite=Strict` + هيدر خاص مطلوب                                                                                                                  | `auth.controller.ts`                                         |
| الحقن (SQL Injection) والمدخلات الغريبة | كل الاستعلامات بمتغيرات منفصلة، وكل مدخل بيتفحص بقواعد صارمة، وأي حقل زيادة بيترفض                                                                       | `packages/shared/src/schemas.ts`                             |
| التلاعب في الأسعار من الموبايل          | الأسعار والإجمالي والعمولة بتتحسب في السيرفر من قاعدة البيانات بس                                                                                        | `orders.service.ts`                                          |
| التلاعب في الفلوس                       | دفتر حسابات بقيد مزدوج، ممنوع تعديله أو مسحه (مفروض جوه القاعدة)، وكل قيد لازم يكون متوازن، ومفيش عملية تتسجل مرتين                                      | `ledger.service.ts`، ترحيل الأمان                            |
| إنكار عملية حصلت                        | سجل بكل عملية حساسة: مين، وإمتى، ومن أنهي جهاز. ممنوع تعديله أو مسحه حتى من صاحب القاعدة                                                                 | `audit.service.ts`                                           |
| تسريب البيانات الحساسة                  | الرقم القومي متشفر بـ AES-256-GCM. العميل مايشوفش عمولة المحل، والمحل مايشوفش رقم العميل                                                                 | `crypto.service.ts`                                          |
| الأسرار في الكود                        | مفيش ولا سر في الكود. السيرفر بيرفض يشتغل لو إعداد ناقص أو ضعيف. فحص تلقائي (gitleaks) بيمنع رفع أي سر                                                   | `config/env.ts`، `.github/workflows/ci.yml`                  |
| رسائل الأخطاء بتكشف تفاصيل              | أي خطأ غير متوقع بيرجع رسالة عامة، والتفاصيل بتتسجل عندنا بس                                                                                             | `http-exception.filter.ts`                                   |
| ثغرات المتصفح (XSS و Clickjacking)      | هيدرات أمان صارمة (CSP و HSTS و X-Frame-Options)، و React بيهرّب أي نص                                                                                   | `bootstrap.ts`، `deploy/Caddyfile`                           |
| إغراق السيرفر بالطلبات                  | حد عام لكل جهاز، وحجم الطلب محدود، ووقت أقصى لكل استعلام                                                                                                 | `ip-rate-limit.guard.ts`، `db.service.ts`                    |
| ثغرات المكتبات                          | فحص تلقائي مع كل تعديل + Dependabot بيحدّث تلقائي + CodeQL بيحلل الكود                                                                                   | `.github/`                                                   |
| ضياع البيانات                           | نسخة احتياطية مشفرة كل يوم، مع بصمة للتأكد إنها سليمة، واتجرب الاسترجاع فعلاً                                                                            | `scripts/`                                                   |
| اختراق السيرفر نفسه                     | قاعدة البيانات و Redis مش ظاهرين للإنترنت، والسيرفر شغال بمستخدم عادي وملفاته للقراءة بس                                                                 | `docker-compose.yml`                                         |

## الاختبارات الأمنية التلقائية (بتشتغل مع كل تعديل)

- شركة مايقدرش مديرها يشوف أو يلغي طلب شركة تانية، ولا يعدّل أسعار مناطقها
- قاعدة البيانات بترجع صفر صفوف من غير ختم شركة، وبترفض كتابة صف باسم شركة تانية
- توكن مزوّر، أو متعدّل فيه الدور، أو من غير توقيع، بيترفض
- كل دور بيترفض من مسارات غيره
- محل مايقدرش يقبل طلب محل تاني، ولا يعدّل منتجاته
- طيار مايقدرش يستلم طلب مش مسند له
- محاولات الحقن بترجع خطأ آمن
- الدفتر ممنوع يتعدل، والقيد غير المتوازن بيترفض، والتسليم مرتين مابيسجلش فلوس مرتين
- قفل الحساب، وحدود الأكواد، وكشف سرقة الجلسة

## ⚠️ حاجات لسه (مخططة)

- **شركة الرسائل:** لحد ما نختارها، أكواد العملاء مش هتتبعت على السيرفر الحقيقي
- **التحقق بخطوتين لمديري الشركات:** مقترح في المرحلة الخامسة
- **الحساب لما يتوقف:** بيخرج من كل الأجهزة، بس التوكن القصير بيفضل شغال لحد ١٥ دقيقة
- **النسخ الاحتياطي خارج السيرفر:** لازم تتظبط يدوي (شوف README)
